{
  "name": "stellar-wallet-dashboard",
  "version": "1.0.0",
  "description": "A Stellar wallet dashboard to view balances, transactions, and send payments",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.3.1"
  }
}
